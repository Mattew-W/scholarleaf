# ============================================================
# 反检测浏览器系统 - 依赖清单
# ============================================================
# 安装: pip install -r requirements.txt
# ============================================================

# 核心引擎（二选一，推荐 patchright）
patchright>=1.43         # 底层修复 Runtime.enable 泄露，反检测效果最优
# playwright>=1.43       # 标准引擎（patchright 不可用时回退）

# 可选反检测增强
playwright-stealth>=1.0.6  # 额外反检测注入
curl-cffi>=0.7            # TLS/JA3 指纹伪装

# 可选：类型检查 & 开发工具
# mypy>=1.8
# ruff>=0.3
